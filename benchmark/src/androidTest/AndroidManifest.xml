<?xml version="1.0" encoding="utf-8"?>

<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="codes.chrishorner.recyclerbenchmark.test"
    >

  <!-- Debugging disabled for accurate performance results. -->
  <application
      android:allowBackup="false"
      android:debuggable="false"
      android:fullBackupContent="false"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/benchmark_appName"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:theme="@style/Theme.RecyclerBenchmark"
      tools:replace="android:debuggable"
      >

    <activity android:name="codes.chrishorner.recyclerbenchmark.ui.MainActivity">

      <intent-filter>

        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
        <category android:name="android.intent.category.DEFAULT" />

      </intent-filter>

    </activity>

  </application>

</manifest>
